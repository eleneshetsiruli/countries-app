import { AboutDescription } from '../components/description';

export const AboutView = () => {
    return (
        <>
            <AboutDescription />
        </>
    );
};

export default AboutView;
